# Random Number
# Python does not have a random() function to generate a random number.
# But it has a built-in module called random that can be used to generate random number

#Example
# generates a random number from 1 to 9
import random as r

print(r.randrange(1,10)) # Generates a random number between 1 and 9 each time the code is runed.

#end